# game-images

게임에 사용할 이미지는 이 폴더 아래에 넣으면 서버가 자동으로 읽습니다.

- 지원 확장자: `.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`, `.svg`
- 기본 샘플: `samples/`
- 직접 추가용 폴더: `user/`

클라이언트는 `object-fit: contain`으로 표시하므로 원본 비율이 유지됩니다.
